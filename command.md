#Git Command

終端機（或是 Bash）是一種只利用鍵盤輸入指令來操控電腦的方式。  
你可以重新命名檔案、開啟檔案、建立新資料夾、並在不同目錄（資料夾）之間切換，全都利用鍵盤輸入指令完成。

建立一個新資料夾 (或稱做目錄)
$ mkdir <FOLDERNAME>

進入一個資料夾 (或稱做切換目錄)
$ cd <FOLDERNAME>

列出資料夾內容
$ ls

為一個資料夾加上 Git 功能
$ git init


$git status
